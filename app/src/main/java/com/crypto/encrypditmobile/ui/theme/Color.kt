package com.crypto.encrypditmobile.ui.theme

import androidx.compose.ui.graphics.Color

val forest = Color(0xFF11201E)
val medForest = forest.copy(alpha = .6f)
val lightForest = forest.copy(alpha = .2f)
val teal = Color(0xFF154042)
val medTeal = teal.copy(alpha = .6f)
val lightTeal = teal.copy(alpha = .2f)
val mint = Color(0xFFD5E9DD)
val medMint = mint.copy(alpha = .6f)
val lightMint = mint.copy(alpha = .2f)
val pink = Color(0xFFDECEC8)
val medPink = pink.copy(alpha = .6f)
val lightPink = pink.copy(alpha = .2f)
val cloud = Color(0xFFD4D6D5)
val medCloud = cloud.copy(alpha = .6f)
val lightCloud = cloud.copy(alpha = .2f)
val darkMocha = Color(0xFF211c1a)
val mocha = Color(0xFF514c4a)
val medMocha = mocha.copy(alpha = .6f)
val lightMocha = mocha.copy(alpha = .2f)
val black = Color.Black